// Production environment configuration
// The API URL will be dynamically set based on the deployed backend URL
export const environment = {
  production: true,
  // Replace with your actual Render backend URL after deployment
  // Format: https://codeq-backend.onrender.com/api
  apiUrl: 'https://codeq-backend.onrender.com/api'
};

